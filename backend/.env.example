# Copy to .env and fill in values
# Get DATABASE_URL from: Supabase dashboard → Project Settings → Database → Connection string (Transaction pooler)
# IMPORTANT: The host segment (e.g. aws-1-ap-southeast-2) must match YOUR project's region exactly.
#            Copy the full connection string from the dashboard — do not guess the region.
# Replace [YOUR-PASSWORD] with your actual DB password
DATABASE_URL=postgresql://postgres.wvmjvigbysxwqahzbkps:[YOUR-PASSWORD]@aws-1-ap-southeast-2.pooler.supabase.com:6543/postgres

CORS_ORIGINS=["http://localhost:3000","http://localhost:3001"]
